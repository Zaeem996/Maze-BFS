This code covers an aspect of maze solving that is using a bfs-algorithm.
main function maze_sol.
valid module is to check wheather transition either up,down,left or right is true or not.
findend functions is used to se wheather the end point is reached or not, as soon as end point is reach on traversing this function calls print function which then line by line print nested lists adding path.
this code can be executed on any pc or mobile that have python installed in it.
